# Docker Compose Environment Variables
# Copy this to .env in the root directory and customize as needed

#==========================================
# Canvas API Configuration (REQUIRED)
#==========================================
CANVAS_API_TOKEN=your_canvas_api_token_here
CANVAS_URL=https://canvas.lms.unimelb.edu.au/api/v1

#==========================================
# Application Configuration
#==========================================
LOG_LEVEL=INFO

# Cron Scheduling (set ENABLE_CRON=true to activate)
ENABLE_CRON=false
CRON_SCHEDULE=0 2 * * *

# Health Check Server
ENABLE_HEALTH_SERVER=true

#==========================================
# Database Configuration (Optional)
#==========================================
# Uncomment and configure if using PostgreSQL profile
# POSTGRES_DB=canvas_scraper
# POSTGRES_USER=canvas
# POSTGRES_PASSWORD=secure_password_here

#==========================================
# Supabase Integration (Optional)
#==========================================
# Uncomment and configure if using Supabase
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_ANON_KEY=your-anon-key
# SUPABASE_SERVICE_KEY=your-service-key

#==========================================
# Docker Deployment Options
#==========================================
# Available profiles: database, queue, development
# Use: docker-compose --profile database --profile queue up
COMPOSE_PROFILES=

# Container resource limits (optional)
# MEMORY_LIMIT=512m
# CPU_LIMIT=1.0